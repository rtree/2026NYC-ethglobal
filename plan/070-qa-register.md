# MVP QA register

This file tracks MVP readiness gaps that are easy to miss in build-only checks. Add one row per
issue, then add or update an automated check so similar UI/API wiring regressions are caught.

## Review rule

When one issue is found, search for the same pattern across the app:

1. UI controls: every button that mutates state must map to an `api.*` client method, a server route,
   and a backend implementation that performs the advertised action.
2. UI data: every non-label value shown to the user must come from `/api/state`, `/api/intents*`, the
   wallet/auth provider, or clearly marked local/dev state. Avoid hardcoded demo strings in live views.
3. Intent context: write APIs that act on a launched intent must carry the current `intentId` unless
   the action is intentionally global and documented.
4. Runtime claims: labels such as "live", "funded", "registered", or "running" must reflect a real
   API/chain/runtime source, not a static phrase.

## Current register

### Outstanding

| ID | Area | Status | Finding | Suspected cause | Proposed fix | Fixer verification | Finder verification | Coverage |
|---|---|---:|---|---|---|---|---|---|
| AUTH-004 | Smart-account sign-in | Fixed (pending user wallet) | The user's wallet `0x7B79…2A2e` has code `0xef0100…` = EIP-7702-delegated (MetaMask Smart Account). The server used standalone EOA-only `verifyMessage` which rejects ERC-1271/6492/7702 signatures at `/api/auth/web3` BEFORE the Firebase step. Now uses a Base `PublicClient.verifyMessage` (RPC fallback across Alchemy/Infura/public) which is ERC-1271/6492/7702-aware, with a local EOA verify fallback if RPC is unreachable. | Smart-account signatures need RPC-backed verification, not standalone ecrecover. | Keep `PublicClient.verifyMessage` with Base RPC fallback. | EOA path verified live (no regression); 7702 path deployed in revision 00010-kvr. | EOA via client verifyMessage = 200 PASS live. Needs a final confirm with the user's actual 7702 wallet now that AUTH-003 no longer blocks sign-in. | Manual live wallet test (user) |
| RUNTIME-001 | OpenClaw runtime provisioning | Open | Despite product copy saying OpenClaw/Cloud Runtime, no separate Cloud Run runtime service/job, Cloud Scheduler job, or Cloud Tasks queue exists. `/api/runtime/start` only records a bounded schedule in Firestore and action log; it does not provision or execute OpenClaw ticks. | MVP implemented a control-panel scheduler record before provisioning the actual runtime capsule. | Either implement a bounded OpenClaw runtime service/job + scheduler/tick endpoint, or keep MVP honest by labeling it as a saved runtime schedule/manual control panel only. | n/a (open) | Verified by reviewer: `runtimeStart()` only writes Firestore; `gcloud run services` shows only `intentos-panel`; Cloud Run jobs list is empty; Cloud Scheduler/Tasks APIs are disabled; no tick/OpenClaw logs found. | gcloud service/job/log inspection |
| RUNTIME-002 | Runtime status semantics | Open | The UI/logic can show `running` once an Executor token exists, even though no OpenClaw runtime is provisioned and no autonomous loop is executing. | `hasActiveIntent()` is session/token based, not runtime-health based. | Rename to `session active` / `agent minted`, or add a real runtime health/status source and derive `running` only from that. | n/a (open) | Verified by reviewer: `hasActiveIntent()` checks only `state.session.executorTokenId`; `runtimeStart()` only saves metadata. | code review |
| IDENTITY-001 | ERC-8004 registration / tokenURI | Open | AgentNFT stores only an owner-set `tokenURI` string; no ERC-8004 registration JSON is generated, frozen, or populated with ENS/agent/runtime fields. | Contract has a generic `setTokenURI()` placeholder but no metadata generation/publication pipeline. | Generate and persist ERC-8004 registration JSON on mint/fix, include ENS name, package/runtime hashes, endpoint/context fields, and set `tokenURI` to that immutable/pinned URI. | n/a (open) | Verified by reviewer: `AgentNFT.sol` only has `_tokenURI` string + `setTokenURI`; no server API calls it and minted token identity is shown as planned. | contract/server code review |
| IDENTITY-003 | AgentNFT image / lightweight on-chain art | Open | Agent NFTs are visually sparse; user requested a lightweight NFT image, ideally on-chain-generated if feasible, to make Executor/Watcher identities feel real. | MVP prioritized mint/bind semantics over metadata/image rendering. | Add a tiny SVG/data-URI renderer or metadata generator for Executor/Watcher tokenURI. If full on-chain art is too much, generate deterministic SVG off-chain and pin/store it, then set `tokenURI`. | n/a (open) | User feedback after minting Executor #10 / Watcher #11: "NFTなのに画像内の寂しい...軽量の画像をOnChainにもってNFTの画像生成できないかな". | user live review |
| IDENTITY-002 | ENS/Basename publication | Open | `agent-<tokenId>.intentos.base.eth` / `watcher-<tokenId>.intentos.base.eth` are displayed as planned strings only; ENS/Basename subnames and ENSIP text records are not written. | No ENS/Basename write path or API exists. | Add subname creation + text-record writes for `agent-context`, `agent-endpoint[web]`, and `agent-registration[...]`, or keep UI explicitly planned. | n/a (open) | Verified by reviewer: LaunchFlow marks names planned; no `api.*` or server route handles ENS/Basename writes. | code review |
| REGISTRY-001 | Runtime Registry / package materialization | Open | AgentNFT stores package hashes, and Firestore stores package contents, but no Runtime Registry resolves `tokenId -> packageHash -> package contents -> runtime workspace` or injects packages into OpenClaw. | On-chain mint and off-chain package authoring were implemented, but the registry/materialization layer is missing. | Implement a Runtime Registry service/table keyed by tokenId/packageHash that materializes the package for a runtime and verifies hash matches before execution. | n/a (open) | Verified by reviewer: `AgentNFT.baseOf` stores hashes; `store()` stores drafts; no registry/spawn/materialize path exists. | code review |
| REGISTRY-002 | Firestore package snapshots for shared runtime | Fixed, pending deploy verification | MVP should not rebuild Cloud Run images per Intent, but package content should still be materialized so Intent/Semantic changes affect OpenClaw context. | Previous runtime prompt used mostly generic BUY/HOLD and conservative Watcher instructions; package hashes were stored but package body was not snapshotted/read by runtime. | On package FIX, save immutable-ish `packageSnapshots/{packageHash}` to Firestore; runtime reads Executor/Watcher snapshots by hash and injects AGENTS.md/SOUL.md/semantic/constraints into OpenClaw prompts. | Store now has `getPackageSnapshot`/`putPackageSnapshot`; `fixPackage()` writes snapshots; `runtimeStart()` records package hashes/semantic snapshots; `runtimeTick()` reads snapshots and injects them into Executor/Watcher prompts; Live Console/Launch show package hash and semantic snapshot. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. | user decision + code fix |
| BINDING-001 | Runtime Binding / transfer invalidation | Open | North Star says runtime authority is valid only while `ownerOf(tokenId) == runtimeOwner` and transfer invalidates old bindings, but authority paths do not check AgentNFT ownership and no automatic transfer hook rotates `bindingNonce`. | BindingNonce exists on the delegate contract, but binding ownership enforcement is not connected to AgentNFT transfers/runtime registry. | Add runtime binding records and enforce `ownerOf(tokenId)==runtimeOwner` in adapter/KMS policy/write paths; rotate/invalidate binding on transfer or require rebind before execution. | n/a (open) | Verified by reviewer: `ExecutionDelegate7702` checks `bindingNonce` but not `ownerOf(tokenId)`; `AgentNFT` has no transfer hook; no runtime registry exists. | contract/runtime code review |
| ARCH-001 | On-chain identity model (demo vs product) | In progress (PRODUCT mode built behind `INTENTOS_OWNER` toggle; pending live wallet test) | All on-chain delegation (EIP-7702) and guarded trades run under the **shared platform demo Owner** EOA `0xeEa9…0f01` (key in Secret Manager), NOT the connected visitor's wallet (e.g. `0x7B79…2A2e`). A user who connects + signs in still only watches an agent operate on the *platform's* funds and identity, which undercuts the "your account, your guardrails" story. The connected address is used for auth + Firestore scoping only, never as the on-chain Owner. | Deliberate demo design keeps all signing server-side (executor SessionKey in KMS + platform relayer) so judges need no funds or wallet signatures. `journey.ts` always builds `c.owner` from `getOwnerAccount()` (Secret Manager `owner-test-wallet-key`). | "PRODUCT mode" (plan/080): connected wallet signs its **own** EIP-7702 authorization in an **Activate** step before the IntentBuilder; thread the connected address through `journey.ts`; user holds ~0.001 USDC + a little Base ETH; the SessionKey/relayer execution pattern keeps working afterward. Keep demo mode as a fallback for judges without funds. | Implemented: server `INTENTOS_OWNER=demo\|connected` toggle, per-address `ctx()`, `GET /api/activate/plan` (unsigned init params), `/api/state?address=`, `ownerMode` in `/api/config`, connected-aware `ensureSetup`/owner-authority guards; app `ActivateGate` does the one-tx `signAuthorization`+`initialize` self-tx before the wizard. Demo default unchanged; typecheck + 19 e2e green. | NOT yet live-tested: needs a fresh plain EOA (avoid overwriting a MetaMask Smart Account) with tiny funds + deploy with `INTENTOS_OWNER=connected`. | Code review + e2e (demo); live per-user test pending |
| GAS-001 | Gas funding UX/model | In progress | Gas Funding shows two lanes and allows manual top-up of both; user found this confusing and questioned whether two lanes are correct. Current contract does have two lanes (`gasVaults()(exec, watcher)`), but product UX needs to explain why or hide implementation detail. Connected mode initially called the server top-up API and failed with "fund your gas vault from your wallet". | Protocol separates executor and watcher reimbursement lanes, but the UI exposed low-level lane accounting without enough context; connected Owner-only top-up must be signed by the user's wallet, not server-side. | Decide product copy/UX: either keep two lanes with clear explanation ("Executor tx gas" vs "Watcher vote gas") or collapse to one "Runtime gas reserve" with advanced details. | Launch UI now uses browser wallet signing for connected-mode `fundGasVault(...)`; server API remains for demo mode. | Verified on-chain after user run: `gasVaults()` for Owner `0x5E90...a221` read executor `0.000400 ETH`, watcher `0.000200 ETH`; deployed revision 00026 contains the connected-mode wallet top-up fix. | Manual review + live chain read + typecheck/build |
| RPC-001 | Write-path RPC reliability | Fixed (pending deploy verification) | Watcher lane top-up returned 500 with `HTTP request failed. Status: 521 URL: https://base.llamarpc`; Cloud Run access log shows `/api/gas/fund` 500 after ~47s. | viem fallback transport can still surface a failing public RPC during write/wait paths; `base.llamarpc.com` appears flaky. | Prefer keyed providers for write-paths, remove or demote `base.llamarpc.com`, and add server logging that records the failing provider/action without exposing secrets. | Code removed `https://base.llamarpc.com` from the public Base RPC fallback list. | Verified by user screenshot + Cloud Run access log; needs redeploy and another top-up/write-path smoke to close fully. Executor top-up succeeded; watcher lane was already funded by Watcher creation. | Cloud Run log + manual RPC review |
| WATCHER-001 | Watcher report/judge actions | In progress | Runtime tick now asks OpenClaw Watcher for a post-trade judgement and records `REPORT_OK` / `REPORT_SUSPICIOUS` / `VOTE_TIGHTEN` in `RuntimeRecord`. If suspicious/tighten is returned, it can call existing `watcherTighten()`. Full report persistence/evidence records are still missing. Manual Live Console Watcher votes were also tested by user and returned reverted txs. | Watcher slice originally implemented only final vote primitives; semantic report pipeline is now minimally bridged in runtime tick. Manual button calls were not passing `intentId`; the failed on-chain txs recovered the correct Watcher KMS signer in trace, so the vote path likely needs live retest after UI/API wiring and nonce fixes. | Persist `WatcherReport` records and tie them to evidence/executionId; expand beyond first-trade conservative tighten; retest manual and autonomous watcher vote live. | Code added `lastWatcherAction`, `lastWatcherTxHash`, `watcherActionsUsed`, max actions, and Live Console display. Revision 00026 passes active `intentId` from Live Console watcher buttons. | Typecheck/build passed; deployed revision 00026. User txs `0x9b252e...3422` and `0x2a23aa...f86b` had receipt status 0 before the fix; `cast call --from relayer` with same calldata recovered watcher key and simulated success, so live retest remains pending. | code review + e2e + live smoke + Base receipt/trace |
| WATCHER-002 | Autonomous semantic monitoring | In progress | Watcher judgement is now invoked automatically after a successful runtime BUY tick when a Watcher exists; it can report/tighten without manual button click. It is not yet a separate Watcher runtime loop consuming historical evidence continuously. | Minimal MVP bridge was added inside Executor runtime tick; separate watcher daemon remains future work. | Split Watcher into its own bounded resident session that reads evidence and reports independently. | Code invokes OpenClaw Watcher prompt after submitted trade and may call `watcherTighten()`. Live Console now also exposes a "Force stop Watcher process" control that stops the shared bounded runtime record for MVP safety. | Typecheck/build passed; deployed revision 00026; autonomous live confirmation still pending because the user's runtime BUY failed before watcher judgement. | code review + e2e + live smoke |
| WATCHER-004 | Watcher decision reason visibility | Fixed, pending deploy verification | User asked why the Watcher tightened after execution. Live Console previously showed only `VOTE_TIGHTEN`, with no rationale. | Runtime stored only the action/tx hash, not a human-readable decision reason; timeline text was generic. | Persist/display a short watcher reason and make timeline copy explain the narrowing. | RuntimeRecord now includes `lastWatcherReason`; autonomous tighten records "observed BUY evidence; future trades capped lower"; Live Console displays `watcher reason`; timeline tighten/freeze messages are more explicit. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. Live record for `intent-90fwtw` confirms OpenClaw BUY + Watcher `VOTE_TIGHTEN` succeeded. | user live review + Firestore runtime record |
| WATCHER-003 | Terminal states / result view completeness | Open | North Star/result flow mentions richer terminal states and result/performance review, but implementation mainly derives `running/frozen` from session+guard and displays a thin history table. | Result UI was merged into Live Console but terminal-state transitions and performance summary were not fully modeled. | Add explicit terminal-state transitions (`owner-stopped`, `fund-exhausted`, transfer/rebind states, etc.) and a real result/performance view backed by chain + store data. | n/a (open) | Verified by reviewer/subagent: shared `TerminalState` is broader than what UI derives; no dedicated result/performance aggregation beyond history list. | code review |
| WORLDID-001 | Human proof | Open (intentional) | World ID stays a dev simulation (sessionStorage flag) by product decision; real IDKit widget is deferred. | Hackathon scope. | Wire `@worldcoin/idkit` when `VITE_WORLDID_APP_ID` is set; gate logic already supports it. | n/a (open) | Not verified as fixed; remains intentionally open. | n/a |
| HISTORY-001 | History provenance | Open | `Your past Intents` is Firestore/off-chain wallet history, not an on-chain provenance view. It can contain drafts/live records independent of final on-chain evidence. | Per-wallet UX history was implemented before on-chain result aggregation. | Rename to `Saved Intents` / `Off-chain history`, or join with on-chain token/evidence/terminal-state data before presenting it as execution history. | n/a (open) | Verified by reviewer/subagent: `LiveConsole` reads `/api/intents`; on-chain timeline is separate and can be empty. | code review |
| UX-002 | Launch flow stale intent/runtime and mint tx UX | Fixed, pending deploy verification | Opening Launch after prior live attempts could preload an old live intent/runtime (`intent-mroaf9`, expired runtime) so Start appeared already scheduled/expired and the new flow looked pre-completed. Mint tx links also disappeared after moving steps because tx hashes were only transient ActionButton state. After Executor #12 mint, the tx succeeded but Launch still showed the Create button because the local IntentDoc was not refreshed. | Launch loaded `r.intents[0]` when no draft existed, used global chain `session` tokenIds instead of the current `IntentDoc`, and `IntentDoc` did not persist mint tx hashes. Create buttons also relied on chain-state invalidation, not refetching the current IntentDoc. | Prefer draft-only on Launch, use current IntentDoc tokenIds, persist mint tx hashes, show Minting state, refresh the IntentDoc immediately after mint, make create APIs idempotent per intent, and allow terminal runtime records to be restarted. | Launch now loads only draft docs by default, step completion uses `intent.executorTokenId`/`intent.watcherTokenId`, Executor/Watcher mint tx hashes persist on the doc and remain linked, ActionButton supports `Minting...`, Watcher identity mirrors Executor fields, expired runtime no longer hides Start, and Executor/Watcher create refetch the IntentDoc immediately. Server create APIs return existing token/tx for an already-minted intent to prevent double minting. | `pnpm typecheck` and `pnpm build` passed after the immediate-refresh/idempotency fix; deploy verification pending. | user live review + code fix |
| RUNTIME-003 | Runtime Registry implementation | In progress | Runtime Registry implementation has started: server/app types now include `RuntimeRecord`, store has `getRuntime`/`putRuntime`, `/api/runtime/start` creates a durable runtime record, `/api/runtime/status` exposes it, and Launch UI displays runtime id/status/tick counters. OpenClaw fund-moving execution is still outstanding. | RUNTIME-001/RUNTIME-002 needed a durable source of truth before real runtime sessions could be launched. | Continue with resident-session lease/idempotency and UI runtime status from `RuntimeRecord`. | Code in progress in `intentTypes.ts`, `store.ts`, `journey.ts`, `server.ts`, app API types, and Launch UI. | Workspace typecheck passed; data-wiring e2e and journey e2e passed; live runtime smoke passed after next slice. | `pnpm typecheck`, `playwright data-wiring`, `playwright journey`, live runtime smoke |
| RUNTIME-004 | OpenClaw Gateway service | Fixed (gateway only) | A real private OpenClaw Gateway Cloud Run service now exists and responds to ready/model/chat smoke with `HOLD`. Control panel server can call it directly using Cloud Run ID token in `X-Serverless-Authorization` and OpenClaw gateway token in `Authorization`. This proves the runtime container + Vertex bridge path is deployable, but fund-moving typed tools are not yet wired. | RUNTIME-001 needed an actual Cloud Run runtime surface before resident session orchestration could be attached. Direct service-to-service calls needed split auth headers because Cloud Run and OpenClaw both use bearer tokens. | Continue by connecting `/api/runtime/run` to real typed tool execution. | Added `openclaw-cloudrun/` wrapper, deploy/smoke scripts, and server `openClawChat()` client; deployed `intentos-openclaw-gateway` revision 00004. | Verified live: direct server client returns `HOLD`; authenticated proxy smoke passes `/readyz`, `/v1/models`, and `/v1/chat/completions`. | live OpenClaw smoke + direct server client smoke |
| RUNTIME-005 | Bounded OpenClaw runtime session | In progress | `/api/runtime/tick` calls the real OpenClaw gateway, updates `RuntimeRecord.executedTicks`/status/budget, and can call the existing guarded `trade()` path on BUY. `/api/runtime/run` loops inside one bounded Cloud Run request until TTL/max ticks/Owner stop. Owner can request runtime stop via `/api/runtime/stop` and Launch UI. First eligible tick can BUY once (`INTENTOS_RUNTIME_FIRST_TICK_BUY=1`), but full typed-tool agent reasoning is still minimal. | This is the bridge slice between registry/status and full autonomous Cloud Run runtime execution. User live run exposed a relayer nonce race on the first fund-moving tick. | Add full typed-tool prompts/tools beyond the initial BUY/HOLD bridge and live-test fund-moving runtime trade by user operation after nonce fix. | Server routes `runtimeRun()`/`runtimeTick()`/`runtimeStop()` + `openClawChat()` are implemented; UI start/stop controls are wired; runtime lease prevents duplicate active run requests while a lease is fresh; Live Console surfaces runtime tick/status/budget and now has force-stop controls. Revision 00026 adds per-relayer nonce locking with pending nonce retry and records tick exceptions as stopped runtime records. | Workspace typecheck/build passed; app e2e passed earlier; deployed revision 00026. User live runtime `rt-intent-mroaf9-8-1781414627402` is no longer running: Firestore status is `expired`, `executedTicks=0/6`, no fresh `/api/runtime/run` activity after the failed request. | `pnpm typecheck`, `pnpm build`, live Cloud Run logs, Firestore runtime record, Base receipt |
| RUNTIME-008 | Runtime status UI polling / TTL expectation | Fixed, pending deploy verification | User observed OpenClaw runtime stuck at `running 0/6` even though it should be executing. Firestore showed the actual latest runtime `rt-intent-90fwtw-14-1781418801976` had already finished as `expired`, `executedTicks=1/6`, `lastTickAction=BUY`, `lastWatcherAction=VOTE_TIGHTEN`, `llmCallsUsed=2`. | Live Console and Launch fetched runtime status once and did not poll; `plannedTicks` is an upper bound, and with a 1-minute TTL a slow BUY + receipt + Watcher judgement can consume the window after one tick. | Poll runtime status while visible and show last action/watcher/updated time so users see completed ticks. Consider clearer copy: planned ticks are maximum possible, not guaranteed. | Live Console and Launch now poll `/api/runtime/status` every 3s; UI shows last action, watcher action/reason, and updated timestamp. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. | Firestore runtime record + code fix |
| RUNTIME-009 | OpenClaw response visibility | Fixed, pending deploy verification | User wants to see what OpenClaw actually replied during Executor and Watcher runtime decisions. Previously Live Console only showed normalized `BUY` / `VOTE_TIGHTEN`, hiding the model response text. | RuntimeRecord stored normalized actions and tx hashes but not the gateway completion text. | Persist bounded raw response snippets from Executor and Watcher OpenClaw completions and show them in Live Console. | RuntimeRecord now stores `lastOpenClawResponse` and `lastWatcherResponse`; Live Console shows `OpenClaw Executor` and `OpenClaw Watcher` rows; Launch summary also shows the latest Executor response. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. Existing live record predates these fields, so next runtime tick will populate them. | user request + code fix |
| UX-004 | Active-intent timeline vs account-level guard state | Fixed, pending deploy verification | New Intents showed previous Intent's evidence/tighten/freeze events, making it look like the old Intent was still displayed. The underlying guard/frozen state is account-level for the Owner EOA, but evidence should be filtered by active intent. | `/api/state` returned all recent delegate logs without an intent filter; GuardTightened/GuardFrozen events do not carry intentId. | Filter EvidenceCommitted by active `intentId` in Live Console and label guard state as account-level when no active evidence exists. | `/api/state` now accepts `intentId`; `getState()` filters EvidenceCommitted by `intentIdHash`; Live Console passes activeIntentId and labels the active timeline. Account-level guard/frozen still displays separately. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. | user live review + code fix |
| API-009 | Connected Owner resume reliability | In progress | Resume/unfreeze from browser wallet can fail with wallet/RPC invalid-parameter errors. | Connected mode must call `ownerUpdateGuard` as an EIP-7702 self-call from the Owner wallet; browser wallet clients are strict about transaction params. | Keep adding wallet-compatible self-call parameters and consider an Activation Kit fallback for ownerUpdateGuard if wallet UX remains unreliable. | Live Console now passes `account: walletClient.account` explicitly on the owner self-call. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending and live retest required. | user live review |
| API-010 | New Intent guard reset | Fixed, pending deploy verification | A new Intent should start from its own FIXed guardrails instead of inheriting the previous Intent's frozen account guard. | EIP-7702 delegate state is account-level; `initialize()` is one-time, so a prior Watcher freeze remains until Owner updates the guard. | Before starting a new runtime, require Owner wallet to submit `ownerUpdateGuard(newIntentGuard)` built from the active Intent's FIXed Executor package. | Added `/api/owner/guard-plan`; Launch Start now signs `ownerUpdateGuard` from the connected wallet before `runtimeStart`, clearing frozen and applying current package constraints. | typecheck/build/deploy pending. | user live review + code fix |
| OPS-001 | Relayer gas runway | Checked | Demo should not fail because the platform relayer hot wallet runs out of ETH. | The relayer fronts gas and is reimbursed from per-Owner gas vault lanes; both relayer ETH balance and vault lanes can limit demos. | Keep relayer funded above demo runway; top up if near ~0.002 ETH. | Current relayer `0x0783...6C9` balance is ~0.00992 ETH; current Owner EOA balance is ~0.00998 ETH; gas vault lanes are exec ~0.000397 ETH and watcher ~0.000199 ETH. No recent gas/insufficient-funds log failures found. | Checked live via `cast balance`, `gasVaults()`, and Cloud Run logs. | live chain read + logs |
| API-008 | Connected Owner resume/unfreeze | Fixed, pending deploy verification | User's Resume / unfreeze failed with `Address "undefined" is invalid` in connected mode. | `/api/state` serialized only token/cap/frozen fields from `HardGuardState`; browser `ownerUpdateGuard` needed `router`, `selector`, and `poolFee`, so `undefined` entered viem ABI encoding. | Include the full guard tuple in `/api/state` and keep the browser owner-signed resume path. | Server state serialization now includes `router`, `selector`, and `poolFee`; app state mapping already consumes those fields. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. | user live review + live `/api/state` read |
| UX-003 | Semantic guardrail editing | Fixed, pending deploy verification | User wants both Executor and Watcher semantic text to be editable before package FIX so packages can be shaped manually. | IntentBuilder generated package drafts but UI did not expose semantic editing. | Add a pre-FIX semantic editor for each package and save drafts before hashing/FIX. | Package cards now show editable semantic guardrails (one per line) before FIX; `/api/intent/semantic` persists changes and refuses edits after FIX. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. | user request + code fix |
| RUNTIME-007 | Relayer nonce/gas race on fund-moving runtime/manual trade | Fixed, pending live retest | User's first OpenClaw runtime run and subsequent manual trade both failed with `nonce too low` (`next nonce 28/29`, submitted nonce 27/28) from relayer `0x0783...6C9`. Later watcher votes mined but reverted with gas limit equal to gas used, indicating under-buffered contract gas estimates. No guarded trade was mined from those failed attempts. | Multiple Cloud Run/API paths sent relayer transactions close together while viem selected a stale nonce; write calls were not serialized by relayer account. viem's write gas estimate was also too tight for watcher vote calls. | Keep relayer tx submission serialized per account, use pending nonce with bounded retry, and over-provision gas on relayed contract writes. Retest live runtime Start -> BUY -> receipt success and manual Watcher vote. | `packages/runtime/src/relay.ts` now wraps relayed contract writes in a per-account nonce lock, reads `getTransactionCount(..., blockTag:"pending")`, retries nonce-too-low errors, estimates gas, and submits with 1.5x + 50k gas buffer. | `pnpm typecheck` and `pnpm build` passed; deploy verification pending. Cloud Run logs captured nonce failures; Base receipts `0xaa3091...5741` and `0xcb2e1c...abde` had status 0 with gasUsed == gasLimit. | Cloud Run logs + Base receipts/tx params + typecheck/build |
| RUNTIME-006 | Vertex budget self-stop | In progress | RuntimeRecord tracks approximate Vertex usage (`llmCallsUsed`, input/output token estimates, estimated cost, budget). Runtime tick self-stops and emits structured Cloud Logging when estimated cost reaches the budget. Default budget is $5/runtime unless `INTENTOS_RUNTIME_MAX_VERTEX_USD` overrides it. | 1-second tick intervals can call Vertex up to 600 times in 10 minutes; without a per-runtime budget, OpenClaw loops could create surprise spend. | Continue by exercising the over-budget path with a low budget in staging and exposing budget state in Live Console. | Code added cost fields, approximate cost accounting, `self-stopped` on budget exhaustion, and `runtime_self_stop` Cloud Logging. | Workspace typecheck and data-wiring e2e passed; live runtime smoke confirmed `llmCallsUsed` and estimated cost increment across OpenClaw ticks. | `pnpm typecheck`, `playwright data-wiring`, live runtime smoke |

### Closed / mitigated

| ID | Area | Status | Finding | Suspected cause | Proposed fix | Fixer verification | Finder verification | Coverage |
|---|---|---:|---|---|---|---|---|---|
| AUTH-001 | API gate | Fixed | Auth now defaults fail-closed on production runtimes; `INTENTOS_AUTH=off` is ignored in production. | Previous dev defaults were allowed to leak into production. | Keep fail-closed default and cover production auth behavior in smoke tests. | Code marks prod fail-closed in `authGate.ts`. | Verified by reviewer code read: `authEnabled()` ignores `off` in production and requires Bearer. | `app/e2e/journey.spec.ts` gate coverage |
| AUTH-002 | Frontend auth config | Fixed | The SERVER is now the single source of truth for "auth required" via public `GET /api/config` (`{authRequired}` from `authEnabled()`). The client reads it (not its own `VITE_FIREBASE_API_KEY`), so the two can't disagree. If the server requires auth but the build lacks a Firebase key, `signInWithWallet` throws loudly ("rebuild with VITE_FIREBASE_API_KEY") instead of creating an empty-token session. The bearer is still attached on the keyed path. | Client and server derived "auth required" independently. | Keep `/api/config` as the source of truth; keep the loud failure. | Code + e2e mock `/api/config` and assert Bearer header. | Verified by reviewer code read: server exposes `/api/config`; client `fetchAuthRequired()` uses it; empty-key path throws when server auth is required. | `app/e2e/data-wiring.spec.ts` (bearer test + `/api/config` mock) |
| AUTH-003 | Deployment auth config | Fixed | The deployed bundle now contains the restricted Firebase Web API key + `signInWithCustomToken`, and the live smoke passes the full handshake. Fixed by `cloudbuild.yaml` that passes the public, API-restricted key as a Docker build arg via a submit-time substitution (key never enters git). SECURITY: the key in the build is `IntentOS browser key (auth only)` — allowlist = `identitytoolkit` + `securetoken` ONLY. We deliberately do NOT use the `Browser key (auto created by Firebase)` because its allowlist includes `firebasevertexai`/`firebaseml` (an AI billing surface on a public key). Vertex conversation stays backend-only via ADC (`/api/intent/chat` -> server -> Vertex). | Vite embeds `VITE_FIREBASE_API_KEY` at build time, but `.env*` is dockerignored so the in-container build shipped without it. | Build via `cloudbuild.yaml` with `_VITE_FIREBASE_API_KEY` substitution from the local gitignored env; deploy the resulting image. | revision 00010-kvr; live smoke ALL PASS; bundle contains `AIzaSyCxFm…` (auth-only key). | Verified live: bundle has the key + `signInWithCustomToken`; smoke passes `signInWithCustomToken` + `/api/intents with bearer`. | `packages/server/src/scripts/smoke.ts` (live) |
| STORE-001 | Persistence | Mitigated | Store now defaults to Firestore in production; explicit `INTENTOS_STORE=memory` still only warns. | Demo/local memory store is still selectable in production. | Refuse `INTENTOS_STORE=memory` on Cloud Run unless a deliberate break-glass env is set. | Code defaults production runtime to Firestore. | Verified by reviewer code read: `store()` selects Firestore by default in production; explicit memory remains warning-only. | Manual review |
| LLM-001 | IntentBuilder | Fixed | Vertex failures log + return `llm:"mock"` (UI labels "scripted"). `INTENTOS_LLM_STRICT=1` makes production hard-fail instead of falling back. | Demo reliability vs production truthfulness. | Default = graceful logged fallback; set `INTENTOS_LLM_STRICT=1` to hard-fail. | Code added logged fallback and strict mode. | Verified by reviewer code read: `INTENTOS_LLM_STRICT=1` rethrows Vertex failures. | Manual review |
| API-001 | Executor create | Fixed | Create Executor button posts `intentId`; server loads FIXed draft and uses package hash/guard. | Earlier create path used hardcoded demo intent/hash. | Keep request-body capture tests for all new create/write actions. | Code + e2e capture current `intentId`. | Verified by reviewer code read: UI sends `intentId`; server routes to `createExecutor({ uid, intentId })`. | `app/e2e/data-wiring.spec.ts` |
| API-002 | Watcher create | Fixed | Create Watcher button posts `intentId`; server binds watcher to the active executor. | Earlier watcher path used static watcher package/hash values. | Keep create watcher tied to stored FIXed packages and active executor token. | Code + e2e capture current `intentId`. | Verified by reviewer code read: UI sends `intentId`; server routes to `createWatcher({ uid, intentId })`. | `app/e2e/data-wiring.spec.ts` |
| API-003 | Gas funding | Fixed | Gas Funding now has explicit top-up buttons wired to `POST /api/gas/fund`. | Funding existed only as implicit setup side effect. | Keep explicit funding buttons and verify request lane + intent context. | Code + e2e capture lane and `intentId`. | Verified by reviewer code read: Funding buttons call `api.fundGas`; server routes to `fundGas(...)`. | `app/e2e/data-wiring.spec.ts` |
| API-004 | Trade/resume intent context | Fixed | Trade and owner resume now accept `intentId` and load the FIXed guard when available. | Write-path endpoints were global/demo scoped. | Keep `intentId` on all launched-intent write APIs. | Code + e2e capture trade `intentId`. | Verified by reviewer code read: trade/resume receive `intentId`; `trade()` loads drafts and uses `intentIdHash(slug)`. | `app/e2e/data-wiring.spec.ts` |
| API-005 | Reset intent context | Fixed | Reset carries the current `intentId` (Intent List loads the active Intent doc); off-chain intent is marked stopped. | Intent List now loads `/api/intents`. | Keep `intentId` on reset. | Code + e2e capture reset `intentId`. | Verified by reviewer code read: Intent List passes `intent?.intentId`; `reset()` marks intent stopped when provided. | `app/e2e/data-wiring.spec.ts` |
| API-006 | Start conditions | Fixed | `POST /api/runtime/start` consumes StartConfig: marks the intent live + records a bounded schedule (startedAt / autoStopAt / plannedTicks, capped at 12). Periodic on-chain execution stays the manual guarded-trade path; a Cloud Scheduler tick is documented future work (must stay bounded). | Runtime launch orchestration now exists. | Keep ticks bounded (MAX_PLANNED_TICKS); a real periodic loop needs Cloud Scheduler/Tasks. | Code + e2e capture runtime start `intentId`. | Verified by reviewer code read: server route exists; `runtimeStart()` caps planned ticks at `MAX_PLANNED_TICKS = 12`. | `app/e2e/data-wiring.spec.ts` (start-runtime test) |
| API-007 | Runtime bounds | Fixed | AgentLoop period must not be below 5 seconds; runtime TTL must not exceed 5 minutes. | Earlier UI/backend allowed 2-second periods and 60-minute TTL, which is too aggressive for Cloud Run/mainnet safety. | Enforce `loopPeriodSec >= 5` and `ttlMinutes <= 5` both in UI inputs and backend clamp. | Code updated in `LaunchFlow.tsx` and `intent.ts`. | Verified by reviewer: workspace typecheck/build passed after the change. | app typecheck/build |
| NFT-001 | AgentNFT mint verification | Fixed | Executor #6 and Watcher #7 are actually minted on Base mainnet and owned by the shared Owner EOA. Watcher #7 is bound to Executor #6 and uses the FIXed package hashes shown in the UI. | User asked whether the UI success reflected real on-chain minting. | Keep post-mint checks in QA when token IDs are shown; consider linking token pages/receipts directly. | n/a (verification row) | Verified by reviewer via Base RPC: `ownerOf(6/7)=0xeEa9...0f01`, `baseOf(6/7)` hashes match UI, `watcherOf(7).watchedExecutorTokenId=6`. | Manual Base RPC read |
| SESSION-001 | Per-user session scoping (connected mode) | Open (post-mechanism; single-user safe) | The module-level `session` in `journey.ts` (`executorTokenId`/`watcherTokenId`/`log`) is a **process-global singleton**. In demo mode this is fine (one shared Owner). In `INTENTOS_OWNER=connected` mode with **concurrent users**, `trade()`/`createWatcher()` read the global `session.executorTokenId`, so User B creating an Executor can make User A's next trade reference the wrong executor tokenId, and `getState()` leaks/returns another user's token IDs. **A single tester (current live test) is unaffected.** | Connected mode was layered on top of the original single-demo session model; token IDs were never scoped per `{uid,intentId}`. `linkIntent()` already persists `executorTokenId`/`watcherTokenId` to the per-user intent doc, so the data exists in Firestore. | In connected mode, read `executorTokenId`/`watcherTokenId` from `store().getIntent(uid,intentId)` (not the global `session`); scope `session.log` per uid or read action history from the intent doc. Do this **after** the per-user 7702 mechanism is proven live (don't add untested code to the critical-path test). | n/a (open) | Found by manager double-check (subagent) 2026-06-14: cites `journey.ts` `session` at the module level, `trade()` using `session.executorTokenId ?? "1"`, `createWatcher()` reading `session.executorTokenId`. | Code review (subagent) |
| GAS-VAULT-001 | Activate vault sizing (connected mode) | Mitigated | `activatePlan()` seeds small gas-vault lanes (exec 0.0004 ETH, watcher 0.0002 ETH = 0.0006 total) so a fresh per-user EOA only needs a tiny ETH balance to Activate (`initialize` requires `exec+watcher <= balance`). `_reimburse()` **reverts** with `GasVaultDepleted` if a trade's gas exceeds the lane, but reimbursement is clamped to `gasPerTxCap = 0.0002 ETH/tx`, so exec 0.0004 guarantees ≥2 trades and (Base gas being ~0.01–0.05 gwei) realistically many more. | Tiny-amounts policy: keep the per-user EOA funding requirement minimal. | Keep small for the live test; surface a "top up your gas vault" hint in the UI (GAS-001) and let the user fund more from their own wallet. Note: a researcher subagent's first estimate ("1–3 trades") used wrong gas arithmetic; real Base gas is far cheaper. | Set in `journey.ts activatePlan().initialize` (exec 0.0004 / watcher 0.0002). | Found via researcher double-check (subagent) 2026-06-14; require statement + clamp verified against `ExecutionDelegate7702.sol`. | Code review (subagent) |
| TX-001 | Receipt status handling | Fixed | A mined transaction can revert with receipt `status=0` while the backend still shows success if code only waits for a receipt. User's later guarded trade tx `0x9c67...e9d9` had `status 0`, no logs, but `/api/state.actions` said "guarded trade executed". | `waitForTransactionReceipt` was treated as success in several helpers unless logs were missing; relay path is now protected but mint/fund/owner helpers also needed status checks. | Check `receipt.status === "success"` for every write helper before returning/logging success. | Runtime helpers now assert success in relay, mint, owner update, delegate/init, fund gas, and deploy paths. | Verified by reviewer: failed tx receipt was status 0; code now throws on non-success receipt before logging ok; workspace typecheck/build passed. | Manual Base receipt read + code review |
| FORK-001 | Local fork execution proof | Closed | Local Base fork E2E proves the core guarded execution stack without real funds: KMS-signed ExecutionRequest swaps USDC->WETH, bounded executor loop adapts after `AmountTooLarge`, and Watcher evidence->tighten->freeze works. | User asked whether remaining MVP paths can be tested without user operation/mainnet funds. | Keep these fork tests current with contract ABI and run them before live fund-moving demos. | Updated runtime fork tests to current `initialize(...)` ABI. | Verified by reviewer: `m1-fork-e2e.ts`, `m1-executor-loop.fork.ts`, `m3-watcher.fork.ts`, `runtime typecheck`, and `contracts:test` all pass on local Base fork. | local anvil Base fork + KMS ADC |
| UX-001 | Post-mint actions | Fixed | After Executor/Watcher mint succeeds, Create buttons should not remain active-looking/clickable. | Button disabled conditions only checked whether the package was FIXed, not whether the agent already existed. | Disable/rename Create buttons after `state.session.executorTokenId` / `watcherTokenId` exists. | Code updated in `LaunchFlow.tsx`: labels become `Executor minted #n` / `Watcher minted #n` and buttons are disabled when token IDs exist. | Verified by reviewer code read; UI redeploy needed. | app typecheck |
| LOG-001 | Server error observability | Fixed | Cloud Run showed `/api/gas/fund` 500 but no app-level error details in logs, making provider/action failures hard to diagnose. | Server write-path catch returned the error to the browser but did not `console.error` it. | Log sanitized route/action + error message in the write-path catch, without secrets or request bodies. | `server.ts` now logs `[api] <METHOD path> failed: <message>` before returning 500. | Verified by reviewer code read and server typecheck. | server typecheck |
| UI-001 | Active Intent card | Fixed | Intent List active card renders the live intent's `intentId` + `title` from `/api/intents`. | Intent List loads the active Intent doc. | Keep API fixture values different from demo constants. | Code + e2e assert API id/title. | Verified by reviewer code read: `IntentList` loads `/api/intents` and renders `cardId`/`cardTitle`. | `app/e2e/data-wiring.spec.ts` |
| UI-002 | Live Console title | Fixed | Live Console heading shows the live intent `intentId` + `title` from `/api/intents`. | Console title derives from the active Intent doc. | Keep title API-driven. | Code + e2e assert API title. | Verified by reviewer code read: `LiveConsole` derives `consoleTitle` from active intent history. | `app/e2e/data-wiring.spec.ts` |
| UI-003 | Owner address display | Fixed | Live Console Owner EOA uses `/api/state.delegate`. | Removed the `ADDR.owner` shortcut in the live view. | Render `state.delegate` for live owner/delegate. | Code + e2e assert fixture delegate. | Verified by reviewer code read: Owner EOA row renders `state.delegate`. | `app/e2e/data-wiring.spec.ts` |
| UI-004 | Runtime/funding claims | Fixed | Funding/launch badges reflect actual vault + `delegated` state (no false "Owner-funded"/"live on Base"). Runtime substrate row labeled as substrate, not a running claim. | Badges now driven by `/api/state`. | Keep badges state-driven. | Code + e2e assert no false funded/live on empty fixture. | Verified by reviewer code read: Funding badge depends on vault balances; live badge depends on `state.delegated`. | `app/e2e/data-wiring.spec.ts` |
| UI-005 | ENS/ERC-8004 registration | Fixed | Identity cards mark ENS/ERC-8004 as **planned** (not claimed-registered) until on-chain; AgentNFT + ExecutionContract shown as real. | No registry read path yet; labels are honest. | Add a registry/ENS read API to upgrade "planned" to verified. | Code relabels registration as planned. | Verified by reviewer code read: identity copy no longer claims registration is published. | Manual review |
| UI-006 | Token pair display | Fixed | Guard/summary token pair derives from `state.guard.tokenA/tokenB` via `tokenSymbol()` (falls back to USDC/WETH for the MVP pair). | Known-token map added in `config.ts`. | Extend the map when new pairs are supported. | Code derives token pair with helper. | Verified by reviewer code read: Live Console and Launch summary use `tokenPair(...)`. | Manual review |
| ROUTE-001 | Direct routes | Fixed | `#/intents`, `#/launch`, `#/console` enforce wallet + Firebase sign-in + World ID via an `App.tsx` route guard; direct links show Onboarding until the gate passes. | Route guard added. | Keep the guard in front of every protected destination. | Code + e2e deep-link test. | Verified by reviewer code read: non-onboarding routes return `Onboarding` unless `useGate().passed`. | `app/e2e/data-wiring.spec.ts` (deep-link test) |
| TEST-001 | Mocked e2e | Mitigated | Existing e2e mocks APIs; data-wiring tests now use non-demo fixture values to catch hardcoded displays. | Earlier UI tests asserted presence, not data provenance. | Keep adding fixture values that differ from demo constants for every live display. | New data-wiring tests exist. | Verified by reviewer: app typecheck passes and 8 data-wiring tests are registered. | `app/e2e/data-wiring.spec.ts` |
| SUPPLY-001 | TanStack supply-chain incident | Closed | Direct dependency `@tanstack/react-query@5.101.0` / `@tanstack/query-core@5.101.0` is installed. Official TanStack postmortem says Query packages were unaffected by the May 2026 compromise, and npm publish times for 5.101.0 are 2026-06-02 (after TanStack's 2026-05-15 all-clear). | Concern came from the TanStack Router/Start npm compromise and broad third-party reporting. | Keep pinned lockfile; avoid affected Router/Start versions; if TanStack advisories change, rotate credentials for any host that installed affected versions. | n/a (verification row) | Verified by reviewer: official TanStack postmortem says Query unaffected/current versions safe; `pnpm view` shows react-query/query-core 5.101.0 published 2026-06-02; lockfile pins 5.101.0. | Manual advisory + lockfile review |
| SUPPLY-002 | Recursive Foundry submodules / halmos-cheatcodes | Mitigated | Top-level clones are only `foundry-rs/forge-std` and `openzeppelin/openzeppelin-contracts`; recursive checkout of OpenZeppelin can bring `a16z/erc4626-tests`, nested `foundry-rs/forge-std`, and `a16z/halmos-cheatcodes`. `halmos-cheatcodes` is public but AGPL-3.0 and not imported by our contracts. | Recursive submodule checkout pulls OpenZeppelin's test/formal-verification dependencies into the working tree even though deployment contracts only need OpenZeppelin `contracts/`. | Do not use recursive submodule checkout in CI/deploy. Top-level `.gitmodules` ignores submodule working-tree dirtiness, and the local nested OpenZeppelin submodules were deinitialized/removed from the working tree. If a future command reintroduces them, remove again before release artifacts. | Nested submodules deinitialized locally; `.gitmodules` now sets `ignore = all` for top-level Forge/OpenZeppelin submodules to avoid dirty state from unused nested submodules. | Verified by reviewer: contracts import only top-level OpenZeppelin/forge-std; `pnpm contracts:build` and `pnpm contracts:test` pass after nested submodule deinit. | submodule/license review + contracts build/test |
| SUPPLY-003 | Dependabot transitive alerts | Open | GitHub Dependabot reports 4 npm alerts in `pnpm-lock.yaml`: `esbuild` (<0.28.1), `ws` (<8.20.1), and `uuid` (<11.1.1). These are transitive through dev tooling / wallet connector stacks. | Current dependency graph pulls vulnerable transitive versions via Vite/tsx (`esbuild`), walletconnect/viem (`ws`), and MetaMask/GCP/wallet stacks (`uuid`). | Prefer upstream dependency updates or Dependabot PRs over broad major-version pnpm overrides; overrides may break wallet connectors (`uuid` 11 is major, `ws` 7->8 can be risky). If urgent, test targeted overrides in a separate PR. | n/a (open) | Verified by reviewer with GitHub Dependabot API and `pnpm why`; no direct dependency uses these packages. | Dependabot API + `pnpm why` |

## Test policy

- Expected-fail tests are intentional for open QA rows. When a row is fixed, remove `test.fail()` from
  the matching test so the fix becomes a normal regression guard.
- New buttons must include a test that captures the request body and verifies the endpoint and
  `intentId`/payload.
- New live displays must be tested with API fixture values that differ from demo constants.

---

## ⚠️ Deploy coordination — READ BEFORE deploying `intentos-panel` (copy-paste to other agents)

There is **ONE** live Cloud Run service and it must stay on **ONE** URL in **connected** (real per-user
EIP-7702) mode. Multiple agents deploying to the same service have repeatedly seized 100% traffic and
silently flipped the live URL back to demo mode. To stop that:

> **Message to paste to any agent/human about to deploy the panel:**
>
> The single live product URL is `https://intentos-panel-41929375451.us-central1.run.app` and it must
> serve **`INTENTOS_OWNER=connected`** (the real per-user EIP-7702 flow + Activation Kit). This is the
> URL judges see — do **not** revert it to demo mode and do **not** create extra tag URLs.
>
> If you deploy `intentos-panel`, you MUST include **all** of these env vars (omitting any will change
> behavior — e.g. dropping `INTENTOS_OWNER` flips it to demo via env, though code now defaults to
> connected on Cloud Run as a backstop):
>
> ```
> --set-env-vars INTENTOS_AUTH=firebase,INTENTOS_STORE=firestore,INTENTOS_LLM=vertex,GOOGLE_CLOUD_PROJECT=ethglobal-nyc2026-rtree,INTENTOS_OWNER=connected,OPENCLAW_GATEWAY_URL=https://intentos-openclaw-gateway-hjx3x7yweq-uc.a.run.app
> ```
>
> Prefer building the image via `cloudbuild.yaml` (it bakes the Firebase Web API key) and deploying that
> image, rather than `--source .`. Use `--max-instances 1` (in-memory nonce store / single-tester demo).
> **After ANY deploy, verify:** `curl -s <URL>/api/config` returns `{"authRequired":true,"ownerMode":"connected"}`.
> If it doesn't, re-pin: `gcloud run services update-traffic intentos-panel --region us-central1 --to-latest`
> on a connected revision, and remove stray tags with `--remove-tags demo,connected`.
>
> If you only need to test something in demo mode, do it **locally**
> (`PANEL_AUTH=off INTENTOS_AUTH=off INTENTOS_STORE=memory INTENTOS_LLM=mock node packages/server/dist/server.js`),
> **not** by deploying demo mode to the shared service.

Backstop in code: `ownerMode()` (in `packages/server/src/journey.ts`) now defaults to **connected on
production runtimes** (Cloud Run) and demo only locally, so a deploy that forgets `INTENTOS_OWNER` still
keeps the live URL on the real path. Keep that default.
