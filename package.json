{
  "name": "intentos",
  "private": true,
  "version": "0.0.0",
  "description": "IntentOS — EIP-7702 guarded-execution layer (ETHGlobal NYC 2026). MVP scope B.",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "contracts:build": "cd contracts && forge build",
    "contracts:test": "cd contracts && forge test -vvv"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
