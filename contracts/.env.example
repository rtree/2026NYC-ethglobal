# Base mainnet RPC (Alchemy/public). Used by forge fork tests and deploy scripts.
BASE_RPC_URL=https://mainnet.base.org
# Basescan API key for verification (optional).
BASESCAN_API_KEY=
# Deployer key is NOT stored here — use a KMS/hardware signer or `cast wallet`. Never commit keys.
